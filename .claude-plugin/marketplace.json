{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "kardebadas-claude-plugin",
  "description": "Personal Claude Code plugins.",
  "owner": {
    "name": "kardebadas"
  },
  "plugins": [
    {
      "name": "bt",
      "source": "./plugins/bt",
      "description": "Personal skill collection, invoked as bt:<skill>. Currently: superpipeline — drives a feature from idea to a finished branch by composing the superpowers skills, with a zero-assumption question law, on-disk run state, two approval gates, reviewer fan-out, a recursive fix loop, and dependency waves for parallel implementers."
    }
  ]
}
